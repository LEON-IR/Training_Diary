<h2>ユーザー情報</h2>

<%= attachment_image_tag current_user, :profile_image_id, :fill, 50, 50, format: 'jpeg', fallback: "no_image.jpg"%>
 <table class="table table-hover">

                        <tbody>
                            <tr>
                                <td><strong>名前</strong></td>
                                <td><strong><%= user.last_name %><%= user.first_name %></strong></td>
                            </tr>
                            <tr>
                                <td><strong>紹介文</strong></td>
                                <td><strong><%= user.introduction %></strong></td>
                            </tr>
                            <tr>
                                <td><p>フォロー数<%= link_to user.followings.count, follows_user_path(user) %></p></td>
                            </tr>
                            <tr>
                                <td><p>フォロワー数<%= link_to user.followers.count, followers_user_path(user) %></p>
                                </td>
                            </tr>
                            <tr>
                                <td>
                                    <% if current_user != user %>
                                        <% if user.followed_by?(current_user) %>
                                            <p><%=link_to "フォロー外す", user_relationships_path(user), method: :delete, class: "btn btn-primary" %></p>
                                        <% else %>
                                            <p><%=link_to "フォローする", user_relationships_path(user), method: :post,class: "btn btn-success" %></p>
                                        <% end %>
                                    <% end %>
                                </td>
                            </tr>
                        </tbody>
                      </table>
<div class="row">
    <%= link_to '', edit_user_path(current_user.id), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench edit_user_#{current_user.id}" %>
</div>